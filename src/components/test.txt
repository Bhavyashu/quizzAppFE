.user-progress-page {
    margin-top: 100px;
    display: flex;
    flex-wrap: wrap;
    flex-direction: column;
    max-height: 100%;
    max-height: 100%;
    /* gap: 100px; */
  }
  
  .language-details {
    flex: 1;
    /* display: flex; */
    /* flex-direction: column; */
    border: 1px solid #ccc;
    padding: 10px;
  }
  
  .exercises-container {
    /* justify-content: center; */
    display: flex;
    flex-wrap: wrap;
    border: 1px solid #ccc;
    flex-direction: row;
    gap: 10px;
  }
  
  .exercise-card {
    flex: 0 0 calc(33.33% - 10px); /* Limit each card to a third of the container width with a 10px gap */
    /* border: 1px solid #ccc; */
    padding: 10px;
    box-sizing: border-box;
    justify-content: center;
  }

  /* Media query for screens smaller than a certain width */
  @media (max-width: 768px) {
    .exercises-container {
      flex-direction: column; /* Change to a column layout on smaller screens */
    }
  
    .exercise-card {
      flex: 0 0 calc(100% - 20px); /* Take up full width with a 20px gap on smaller screens */
    }
  }
  